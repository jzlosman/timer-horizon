{
  "name": "timer-horizon",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css dist/ && cp -R src assets dist/",
    "test": "node --test",
    "check": "node --check src/*.mjs && node --check tests/*.mjs"
  }
}
